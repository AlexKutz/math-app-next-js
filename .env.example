NEXTAUTH_URL="http://localhost:3000"

EMAIL_FROM=""
EMAIL_SERVER_HOST=""
EMAIL_SERVER_PORT=""
EMAIL_SERVER_USER=""
EMAIL_SERVER_PASSWORD=""

# Google OAuth Configuration
AUTH_GOOGLE_ID=""
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""
AUTH_GOOGLE_SECRET=""

# GitHub OAuth Configuration
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""

# Facebook OAuth Configuration
AUTH_FACEBOOK_ID=""
AUTH_FACEBOOK_SECRET=""

# JWT Secret for Authentication
AUTH_SECRET=""

# PostgreSQL Database Configuration

AUTH_RESEND_KEY=""
AUTH_RESEND_FROM=""

# This was inserted by `prisma init`:
# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

# Old configuration to connect to a local PostgreSQL database:
# AUTH_DATABASE_HOST="localhost"
# AUTH_DATABASE_PORT="5432"
# AUTH_DATABASE_NAME="db"
# AUTH_DATABASE_USER="user"
# AUTH_DATABASE_PASSWORD="password"
# DATABASE_URL="postgresql://user:password@localhost:5432/db"

# New configuration to connect to a local Supabase database through Supavisor (Pooler):
AUTH_DATABASE_HOST="localhost"
# Port 6543 is the Supavisor (Pooler) port defined in your Supabase .env
AUTH_DATABASE_PORT="6543" 
# The default database in Supabase is 'postgres'
AUTH_DATABASE_NAME="postgres"
# This matches POSTGRES_PASSWORD from Supabase .env
AUTH_DATABASE_PASSWORD="your-super-secret-and-long-postgres-password"
# Updated Connection String
# Note: ?pgbouncer=true is often required if you are using Prisma with Supavisor
# Used by the App (Port 6543 - Transaction Mode)
DATABASE_URL="postgresql://postgres.your-tenant-id:your-super-secret-and-long-postgres-password@localhost:6543/postgres?pgbouncer=true"
# DIRECT CONNECTION (For Migrations)
# Used by Migrations (Port 5432 - Session Mode)
DIRECT_URL="postgresql://postgres.your-tenant-id:your-super-secret-and-long-postgres-password@localhost:5432/postgres"